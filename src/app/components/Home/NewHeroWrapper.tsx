'use client';

import React from 'react';
import NewHero from './NewHero';

export default function NewHeroWrapper() {
  // No loading, no preloading, just render the hero component immediately
  return <NewHero />;
} 