'use client'
import { motion } from "framer-motion";
import FormService from "./modal";

const AnimatedButton = () => {
  return (
      <AIButton />
  );
};

const AIButton = () => {
  return (
    <FormService buttonText="Strat Optimizing your !" />
  );
};

export default AnimatedButton;